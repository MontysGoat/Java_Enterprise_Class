CREATE TABLE APP.COURSES (COURSEID INTEGER NOT NULL, COURSE_NAME VARCHAR(64) NOT NULL, PRIMARY KEY (COURSEID), UNIQUE (COURSE_NAME));

CREATE TABLE APP.REGISTRAR (COURSEID INTEGER NOT NULL, NUMBER_STUDENTS_REGISTERED INTEGER NOT NULL, PRIMARY KEY (COURSEID));qqui